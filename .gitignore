/target
Cargo.lock.orig
tools/bench/media/
tools/bench/results/
node.toml
# Réglages de sortie persistés par le node (écran/plein écran choisis dans l'UI).
sortie.json
# Interrupteurs de fonctions persistés par le node (onglet Fonctions).
fonctions.json
/media/
/presets/
/logs/
webui/node_modules/
webui/dist/
# Outillage local des sessions Claude Code (launch.json…).
.claude/
# Console lumieres persistee par le node (page Lumieres).
lumieres.json
# Sequences persistees par le node (page Sequences).
sequences.json
