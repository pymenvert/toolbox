/target
Cargo.lock.orig
tools/bench/media/
tools/bench/results/
node.toml
# Réglages de sortie persistés par le node (écran/plein écran choisis dans l'UI).
sortie.json
# Interrupteurs de fonctions persistés par le node (onglet Fonctions).
fonctions.json
/media/
/presets/
/logs/
webui/node_modules/
webui/dist/
# Outillage local des sessions Claude Code (launch.json…).
.claude/
# Console lumieres persistee par le node (page Lumieres).
lumieres.json
# Sequences persistees par le node (page Sequences).
sequences.json
# Etat de demarrage enregistre depuis l'UI.
demarrage.json
reglages.json
# SDK NDI proprietaire (licence NewTek/Vizrt) : JAMAIS versionne.
"NDI sdk/"
NDI?sdk/
# Rapports d'audit et de session : ils detaillent des failles (corrigees, mais
# une feuille de route pour qui reste sur une ancienne version) et restent
# donc HORS du depot public, dans le dossier parent du projet.
AUDIT_*.md
RAPPORT_*.md
